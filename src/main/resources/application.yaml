spring:
  application:
    name: company-service

  datasource:
    url: jdbc:postgresql://localhost:${DB_PORT:5432}/${DB_NAME:tripmate_postgres}
    username: ${DB_USER:tripmate}
    password: ${DB_PASSWORD}
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update    # create / validate
    show-sql: true
    properties:
      hibernate:
        default_schema: company
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect

server:
  port: 0

eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_SERVER_URL:http://localhost:8761/eureka}
  instance:
   instance-id: ${spring.cloud.client.hostname}:${spring.application.instance_id:${random.value}}

#eureka:
#  client:
#    service-url:
#      defaultZone: https://dolphin-device-evident.ngrok-free.dev/eureka/
#      fetch-registry: true

#  instance:
#    hostname: "resupply-botch-styling.ngrok-free.dev"
#    non-secure-port: 80

#    prefer-ip-address: false